# =============================================================================
# Python dependencies for the trading bot
# =============================================================================

# Alpaca brokerage SDK — orders, positions, account data
alpaca-py==0.34.0

# Data manipulation — the backbone of all indicator math
pandas==2.2.3

# Supabase Python client — REST + real-time over Postgres
supabase==2.15.2

# Advanced job scheduler — runs the 5-min loop with cron-like precision
APScheduler==3.10.4

# Loads .env files when running locally (no-op on Railway)
python-dotenv==1.0.1

# Timezone-aware datetime handling — critical for ET market hours
pytz==2024.2

# HTTP client (used internally by several dependencies)
requests==2.32.3

# numpy is required by pandas — pinning for reproducibility
numpy==1.26.4
