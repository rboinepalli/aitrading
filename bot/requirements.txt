# =============================================================================
# Momentum Scanner Bot — Python dependencies
# =============================================================================

# Alpaca brokerage SDK — orders, positions, quotes, historical bars
alpaca-py==0.34.0

# Finnhub client — stock screener, top movers, company news
finnhub-python==2.4.20

# Technical indicators — EMA, RSI, ATR, VWAP via pandas-ta
pandas-ta==0.3.14b0

# Telegram bot — trade alerts + YES/NO approval flow
python-telegram-bot==21.6

# Data manipulation
pandas==2.2.3

# Supabase Python client
supabase==2.15.2

# Job scheduler — scan schedule on Railway
APScheduler==3.10.4

# .env file loader (no-op on Railway)
python-dotenv==1.0.1

# Timezone handling — ET market hours
pytz==2024.2

# HTTP client
requests==2.32.3

# numpy pinned for pandas compatibility
numpy==1.26.4
